import styled from "styled-components"

export default styled.button`
  appearance: none;
  background: black;
  color: white;
  font-size: 1em;
  font-family: inherit;
  border: 0 none;
  padding: 10px 15px;
  text-transform: uppercase;
`
